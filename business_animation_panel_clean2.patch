--- a/pysi/gui/business_animation/business_animation_panel.py
+++ b/pysi/gui/business_animation/business_animation_panel.py
@@ -401,10 +401,10 @@
             # If coordinates are too small, treat them as logical coords and scale them.
             # Example: 0..5 / -1..1 style positions from merged layout.
             if max_abs_x <= 20 and max_abs_y <= 20:
-                left_pad = 120
-                top_pad = 160
-                x_scale = 190
-                y_scale = 140
+                left_pad = 140
+                top_pad = 280
+                x_scale = 170
+                y_scale = 160
 
                 min_x = min(xs) if xs else 0.0
                 min_y = min(ys) if ys else 0.0
